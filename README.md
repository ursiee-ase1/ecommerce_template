# ecommerce_template

This repository contains a Django-based e-commerce template project.

Quick start

- Create a virtual environment and install dependencies: `pip install -r requirements.txt`
- Do not commit `db.sqlite3` or your media files; they are ignored by `.gitignore`.

See project docs in the repo for more details.
